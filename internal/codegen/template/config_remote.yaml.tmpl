# Remote config is disabled by default.
# Enable it after adding the matching provider import/dependency, for example xkitpkg/config/etcd.
# config:
#   type: "etcd"
#   etcd:
#     endpoints:
#       - "localhost:2379"
#     timeout: "5s"
#     key: "/{{ .AppName }}"
